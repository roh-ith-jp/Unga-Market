{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key for AI-powered product image generation. Obtain from Google AI Studio (https://aistudio.google.com/apikey). Optional at boot — the app falls back to generated SVG packshots when absent.",
      "requiredAtBoot": false
    }
  ]
}
